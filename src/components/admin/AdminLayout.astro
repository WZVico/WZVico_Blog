---
import BaseLayout from '../../layouts/BaseLayout.astro';

const { title, stylesHref = null, bodyClass = 'admin-page' } = Astro.props as {
  title: string;
  stylesHref?: string | readonly string[] | null;
  bodyClass?: string;
  adminSidebarScope?: string;
};

const stylesheetHrefs = Array.isArray(stylesHref)
  ? stylesHref.filter((href): href is string => typeof href === 'string' && href.length > 0)
  : typeof stylesHref === 'string' && stylesHref.length > 0
    ? [stylesHref]
    : [];
---

<BaseLayout title={title} bodyClass={bodyClass} enableShellScripts={import.meta.env.DEV}>
  <Fragment slot="head">
    <meta name="robots" content="noindex,nofollow" />
    {stylesheetHrefs.map((href) => <link rel="stylesheet" href={href} />)}
    <slot name="head" />
  </Fragment>
  <slot />
</BaseLayout>
